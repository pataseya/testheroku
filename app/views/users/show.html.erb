<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Team:</strong>
  <%= @user.team_id %>
</p>

<% if !@user.avatar.url.nil? %>
<p>

  <%= image_tag @user.avatar.url, size: "150x200" %>

</p>
  <% end %>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
